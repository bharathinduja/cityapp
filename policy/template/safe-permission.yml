#Grant application access to credentials synchronized form CyberArk Vault
- !grant
  role: !group {{ VCS_VAULT }}/{{ VCS_LOB }}/{{ VCS_SAFE }}/delegation/consumers
  members:
  - !layer /openshift-apps/cityapp

